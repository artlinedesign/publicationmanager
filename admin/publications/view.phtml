<?php

require plugin_dir_path( __FILE__ ) . 'model.php';


require plugin_dir_path(__FILE__) . 'class-publications-list.php';

$list = new Publications_List();

if((isset($_GET['action']) && $_GET['action'] === 'add') && (isset($_POST['submit']) && $_POST['submit'] === 'Submit')){
    createVerlag();
}


?>


<div class="wrap">
    <h2>Publikationen</h2>

    <div id="poststuff">
        <div id="post-body" style="width:100%" class="metabox-holder columns-2">
            <div id="post-body-content">
                <div class="meta-box-sortables ui-sortable">
                    <form method="post">
                        <?php
                        $list->prepare_items();
                        $list->display(); ?>
                    </form>
                </div>
            </div>
        </div>
        <br class="clear">
    </div>
</div>

<form action="admin.php?page=verlaege&action=add" method="POST">
    <?php echo Formbuilder::addInput("name", "text", "author-input", "name-input", "Name"); ?>
    <?php echo Formbuilder::addInput("submit", "submit", "author-button", "submit_authors", null, "Submit"); ?>
</form>
